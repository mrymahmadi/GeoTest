/*export const isRingClosed = (rings) => {
  if (!Array.isArray(rings) || rings.length < 4) return false;
  const [lon1, lat1] = rings[0];
  const [lon2, lat2] = rings[rings.length - 1];
  return lon1 === lon2 && lat1 === lat2;
};
*/
